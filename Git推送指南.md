# 📤 Git 推送到 GitHub 指南

## ✅ 已完成配置

你的项目已成功推送到 GitHub！

**仓库地址：** https://github.com/ffzzffz/studyroom.git

---

## 📝 查看在线仓库

访问以下地址查看你的项目：

```
https://github.com/ffzzffz/studyroom
```

---

## 🔄 日常更新推送

### 方法1：使用命令行（推荐）

```bash
# 1. 进入项目目录
cd "E:\codes\idea workspace\studyroom"

# 2. 查看修改状态
git status

# 3. 添加所有修改
git add -A

# 4. 提交修改（带说明）
git commit -m "你的修改说明"

# 5. 推送到 GitHub
git push
```

### 方法2：使用 IDEA Git 工具

1. 在 IDEA 中打开项目
2. 点击菜单：`VCS → Git → Commit`
3. 选择要提交的文件
4. 输入 Commit Message
5. 点击 `Commit and Push`

### 方法3：使用批处理文件

我已为你创建了一个快速推送脚本，执行以下步骤：

1. 双击运行：`快速推送到GitHub.bat`
2. 输入提交说明
3. 自动完成添加、提交、推送

---

## 📋 常用 Git 命令

### 查看状态
```bash
git status                 # 查看修改状态
git log --oneline         # 查看提交历史
git remote -v             # 查看远程仓库
```

### 拉取更新
```bash
git pull                  # 拉取远程更新
git pull origin master    # 拉取指定分支
```

### 分支管理
```bash
git branch                # 查看本地分支
git branch dev            # 创建新分支 dev
git checkout dev          # 切换到 dev 分支
git merge dev             # 合并 dev 分支到当前分支
```

### 撤销修改
```bash
git restore <file>        # 撤销文件修改
git reset HEAD~1          # 撤销最后一次提交（保留修改）
git reset --hard HEAD~1   # 撤销最后一次提交（删除修改）
```

---

## 🚀 克隆项目到其他电脑

如果要在其他电脑上克隆这个项目：

```bash
# 克隆仓库
git clone https://github.com/ffzzffz/studyroom.git

# 进入项目目录
cd studyroom

# 查看分支
git branch -a
```

---

## 🔒 配置 Git 用户信息

如果是第一次使用 Git，需要配置用户信息：

```bash
# 配置用户名
git config --global user.name "你的用户名"

# 配置邮箱
git config --global user.email "你的邮箱@example.com"

# 查看配置
git config --list
```

---

## 📊 GitHub 仓库设置

### 设置仓库描述

1. 访问 https://github.com/ffzzffz/studyroom
2. 点击右上角的 `Settings`
3. 在 `Description` 中输入项目描述
4. 添加 Topics 标签（如：java, vue, spring-boot）

### 开启 GitHub Pages（可选）

如果想部署前端页面：

1. 进入仓库 `Settings`
2. 找到 `Pages` 选项
3. 选择分支和目录
4. 保存后即可访问

---

## 🎯 .gitignore 文件说明

已为你配置好 `.gitignore`，以下文件/目录会被忽略：

- ✅ `node_modules/` - Node.js 依赖
- ✅ `target/` - Maven 编译输出
- ✅ `.idea/` - IDEA 配置文件
- ✅ `*.log` - 日志文件
- ✅ `.DS_Store` - Mac 系统文件

---

## 📞 遇到问题？

### 推送失败？

```bash
# 先拉取远程更新
git pull origin master

# 解决冲突后再推送
git push
```

### 账户认证失败？

GitHub 已不再支持密码认证，需要使用：

1. **Personal Access Token（推荐）**
   - 访问：https://github.com/settings/tokens
   - 生成新 Token
   - 在推送时使用 Token 作为密码

2. **SSH 密钥**
   - 生成 SSH 密钥：`ssh-keygen -t rsa -b 4096`
   - 添加到 GitHub：https://github.com/settings/keys

---

## ✨ 项目已推送成功！

你的项目现在可以通过以下地址访问：

**GitHub 仓库：** https://github.com/ffzzffz/studyroom

**本地项目：** E:\codes\idea workspace\studyroom

---

**祝使用愉快！** 🎉

