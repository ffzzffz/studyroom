# Vue 前端依赖安装 - 手动指令

## 🎯 完整手动安装步骤

### 前提条件检查

打开命令提示符（CMD）或 PowerShell，执行以下命令检查环境：

```bash
# 检查 Node.js 版本
node -v
# 应该显示：v22.21.1 或其他版本

# 检查 npm 版本
npm -v
# 应该显示 npm 版本号
```

---

## 📋 方式1：复制粘贴命令（最简单）

### 步骤1：打开命令提示符

1. 按 `Win + R`
2. 输入 `cmd`
3. 按回车

### 步骤2：复制并执行以下命令

```bash
cd /d "E:\codes\idea workspace\studyroom\studyroom-vue"
```

```bash
rd /s /q node_modules
```

```bash
del /f /q package-lock.json
```

```bash
npm install
```

**等待安装完成（2-3分钟）**

### 步骤3：验证安装

```bash
npm list sass sass-loader vue
```

应该看到：
```
studyroom@1.2.2
├── sass@1.32.0
├── sass-loader@10.1.0
└── vue@2.5.16
```

### 步骤4：启动项目

```bash
npm run dev
```

---

## 📋 方式2：逐步执行（推荐新手）

### 第1步：进入前端目录

```bash
# 方法1：使用 cd 命令（如果路径有空格，需要加引号）
cd "E:\codes\idea workspace\studyroom\studyroom-vue"

# 方法2：使用 /d 参数（推荐，可以跨盘符）
cd /d "E:\codes\idea workspace\studyroom\studyroom-vue"
```

**验证：** 命令提示符应该显示当前路径为 studyroom-vue 目录

---

### 第2步：删除旧的 node_modules（如果存在）

```bash
# Windows CMD
rd /s /q node_modules

# PowerShell
Remove-Item -Recurse -Force node_modules
```

**说明：**
- `/s` = 删除目录及其所有内容
- `/q` = 静默模式，不询问确认

**如果提示"找不到"：** 没关系，说明目录不存在，继续下一步

---

### 第3步：删除 package-lock.json（如果存在）

```bash
# Windows CMD
del /f /q package-lock.json

# PowerShell
Remove-Item -Force package-lock.json
```

**如果提示"找不到"：** 没关系，继续下一步

---

### 第4步：安装依赖

```bash
npm install
```

**这一步会：**
1. 读取 package.json
2. 下载所有依赖包
3. 安装到 node_modules 目录
4. 生成 package-lock.json

**预计时间：** 2-3 分钟（取决于网络速度）

**可能看到的内容：**
```
npm WARN deprecated ...  ← 这些警告是正常的，可以忽略
npm WARN ...

added 1234 packages from 567 contributors in 123s  ← 成功！
```

---

### 第5步：验证安装

```bash
# 查看已安装的关键包
npm list sass
npm list sass-loader
npm list vue

# 或一次查看所有
npm list sass sass-loader vue --depth=0
```

**预期输出：**
```
studyroom@1.2.2
├── sass@1.32.0
├── sass-loader@10.1.0
└── vue@2.5.16
```

**如果显示 UNMET DEPENDENCY 或 missing：** 说明安装失败，请查看"故障排除"部分

---

### 第6步：启动开发服务器

```bash
npm run dev
```

**预期输出：**
```
> studyroom@1.2.2 dev E:\codes\idea workspace\studyroom\studyroom-vue
> webpack-dev-server --inline --progress --config build/webpack.dev.conf.js

 DONE  Compiled successfully!

 I  Your application is running here: http://localhost:8001
```

**成功标志：**
- 看到 "Compiled successfully!"
- 显示地址 http://localhost:8001

---

## 🛠️ 故障排除

### 问题1：npm install 失败

**错误：** `npm ERR! code ENOTFOUND`

**解决：** 网络问题，更换镜像源

```bash
# 使用淘宝镜像
npm config set registry https://registry.npmmirror.com

# 重新安装
npm install
```

---

### 问题2：安装很慢

**解决：** 临时使用镜像

```bash
npm install --registry=https://registry.npmmirror.com
```

---

### 问题3：权限错误

**错误：** `npm ERR! Error: EPERM: operation not permitted`

**解决：** 以管理员身份运行命令提示符

1. 右键点击"命令提示符"
2. 选择"以管理员身份运行"
3. 重新执行安装命令

---

### 问题4：端口被占用

**错误：** `Error: listen EADDRINUSE :::8001`

**解决：** 端口 8001 被占用

```bash
# 查看占用端口的进程
netstat -ano | findstr :8001

# 结束进程（替换 <PID> 为实际进程ID）
taskkill /PID <PID> /F
```

---

### 问题5：SASS 相关错误

如果安装后仍有 sass 相关错误：

```bash
# 确认 package.json 已修改
type package.json | findstr sass

# 应该显示：
# "sass": "^1.32.0",
# "sass-loader": "^10.1.0",
```

如果显示的是 `node-sass`，说明 package.json 没有保存修改：

```bash
# 手动编辑 package.json
notepad package.json
```

找到这两行：
```json
"node-sass": "^4.14.1",
"sass-loader": "6.0.6",
```

改为：
```json
"sass": "^1.32.0",
"sass-loader": "^10.1.0",
```

保存后重新执行 `npm install`

---

## 📊 完整命令序列（一次性执行）

如果你想一次性执行所有命令，可以复制以下内容：

```bash
cd /d "E:\codes\idea workspace\studyroom\studyroom-vue" && rd /s /q node_modules 2>nul && del /f /q package-lock.json 2>nul && npm install && npm run dev
```

**说明：**
- `&&` = 前一个命令成功后才执行下一个
- `2>nul` = 忽略错误输出（如文件不存在）

---

## ✅ 成功验证清单

安装成功后，应该满足以下条件：

- [ ] `node_modules` 目录已创建
- [ ] `node_modules\sass` 目录存在
- [ ] `node_modules\sass-loader` 目录存在
- [ ] `node_modules\vue` 目录存在
- [ ] `package-lock.json` 文件已创建
- [ ] `npm run dev` 成功启动
- [ ] 浏览器访问 http://localhost:8001 显示页面

---

## 🎯 后续步骤

### 启动后端（如果还没启动）

```bash
# 新开一个命令提示符窗口
cd /d "E:\codes\idea workspace\studyroom\studyroom-java"
mvn spring-boot:run
```

### 访问系统

- **前端：** http://localhost:8001
- **后端：** http://localhost:9999/self-study
- **Swagger：** http://localhost:9999/self-study/swagger-ui.html
- **Druid：** http://localhost:9999/self-study/druid/

---

## 📝 备忘录

### 常用命令

```bash
# 启动前端
npm run dev

# 停止服务（在运行窗口按 Ctrl+C）

# 重新安装依赖
npm install

# 清理并重装
rd /s /q node_modules && del package-lock.json && npm install

# 查看已安装的包
npm list --depth=0

# 更新 npm
npm install -g npm@latest
```

---

**现在开始执行吧！复制上面的命令，一步一步来！** 🚀

**如果遇到问题，查看"故障排除"部分。** 💡

