# 快速安装 - 复制粘贴命令

## 🚀 最简单的方法（3条命令）

### 1. 进入目录
```bash
cd /d "E:\codes\idea workspace\studyroom\studyroom-vue"
```

### 2. 清理旧依赖
```bash
rd /s /q node_modules & del /f /q package-lock.json
```

### 3. 安装依赖
```bash
npm install
```

**等待 2-3 分钟，看到 "added xxx packages" 即成功！**

---

## ▶️ 启动项目

```bash
npm run dev
```

**看到 "Compiled successfully!" 即成功！**

**访问：** http://localhost:8001

---

## 🔧 如果安装失败

### 使用国内镜像

```bash
npm install --registry=https://registry.npmmirror.com
```

---

## 📋 一行命令搞定（高级）

```bash
cd /d "E:\codes\idea workspace\studyroom\studyroom-vue" && rd /s /q node_modules 2>nul && del /f /q package-lock.json 2>nul && npm install && npm run dev
```

复制上面这行，粘贴到命令提示符，回车即可！

---

## ✅ 验证安装

```bash
npm list sass sass-loader vue
```

应该显示：
- sass@1.32.0 ✅
- sass-loader@10.1.0 ✅
- vue@2.5.16 ✅

---

**就这么简单！** 🎉

