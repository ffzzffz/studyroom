# 项目接口测试指南

## 🎉 项目已成功启动！

**访问地址：** http://localhost:9999/self-study

---

## 🏠 快速访问

### 系统首页
```
✅ http://localhost:9999/self-study/
```
显示欢迎页面，包含系统状态和快捷链接

### 健康检查接口
```
✅ GET http://localhost:9999/self-study/health
```
**返回示例：**
```json
{
  "code": 0,
  "msg": "success",
  "data": {
    "status": "UP",
    "service": "自习室座位管理系统",
    "version": "1.0.0",
    "time": "2025-11-28 10:20:00"
  }
}
```

### 欢迎信息接口
```
✅ GET http://localhost:9999/self-study/welcome
```
**返回示例：**
```json
{
  "code": 0,
  "msg": "success",
  "data": {
    "title": "欢迎使用自习室座位管理系统",
    "description": "提供自习室座位预约、管理等功能",
    "swagger": "/self-study/swagger-ui.html",
    "druid": "/self-study/druid/",
    "version": "1.0.0"
  }
}
```

---

## 📋 系统文档

### Swagger API 文档
```
✅ http://localhost:9999/self-study/swagger-ui.html
```
打开此链接可以看到所有可用的 API 接口文档

### Druid 数据库监控
```
✅ http://localhost:9999/self-study/druid/
```
可以监控 SQL 执行情况、数据源状态等

---

## 📱 小程序接口测试

### 📢 公告管理

**获取公告列表**
```
GET http://localhost:9999/self-study/applet/listNotice?page=1&limit=10
```

**返回示例：**
```json
{
  "code": 0,
  "msg": "success",
  "data": {
    "total": 3,
    "pageSize": 10,
    "currentPage": 1,
    "totalPage": 1,
    "list": [
      {
        "id": 1,
        "title": "欢迎使用自习室预约系统",
        "content": "请各位同学文明学习，保持安静，爱护公共设施。",
        "createTime": "2025-11-28 10:00:00",
        "status": 1
      }
    ]
  }
}
```

---

### 🏢 楼层管理

**获取所有楼层**
```
GET http://localhost:9999/self-study/applet/getAllFloor
```

**返回示例：**
```json
{
  "code": 0,
  "msg": "success",
  "data": [
    {"id": 1, "floorName": "一楼", "floorNum": 1, "status": 1},
    {"id": 2, "floorName": "二楼", "floorNum": 2, "status": 1},
    {"id": 3, "floorName": "三楼", "floorNum": 3, "status": 1},
    {"id": 4, "floorName": "四楼", "floorNum": 4, "status": 1},
    {"id": 5, "floorName": "五楼", "floorNum": 5, "status": 1}
  ]
}
```

---

### 🚪 自习室管理

**根据楼层获取自习室**
```
GET http://localhost:9999/self-study/applet/getRoomByFloor?id=1
```

**返回示例：**
```json
{
  "code": 0,
  "msg": "success",
  "data": [
    {"id": 1, "roomName": "101自习室", "floorId": 1, "capacity": 50, "status": 1},
    {"id": 2, "roomName": "102自习室", "floorId": 1, "capacity": 40, "status": 1}
  ]
}
```

---

### 💺 座位管理

**根据自习室获取座位**
```
GET http://localhost:9999/self-study/applet/getSeatByRoom?id=1
```

**返回示例：**
```json
{
  "code": 0,
  "msg": "success",
  "data": [
    {"id": 1, "seatNum": "A01", "roomId": 1, "status": 1},
    {"id": 2, "seatNum": "A02", "roomId": 1, "status": 1},
    {"id": 3, "seatNum": "A03", "roomId": 1, "status": 1}
  ]
}
```

---

### 👥 用户管理

**查看封禁用户列表**
```
GET http://localhost:9999/self-study/applet/listForBan
```

**获取留言列表**
```
GET http://localhost:9999/self-study/applet/MessageList
```

---

## 🔐 需要登录的接口

以下接口需要在请求头中携带 token：
```
Headers: token=你的token值
```

### 📅 预约管理

**创建预约**
```
POST http://localhost:9999/self-study/applet/appointment
Headers: token=你的token值
Body:
{
  "seatId": 1,
  "appointmentDate": "2025-11-28",
  "startTime": "08:00",
  "endTime": "12:00"
}
```

**查看我的预约**
```
POST http://localhost:9999/self-study/applet/myAppointment
Headers: token=你的token值
```

**取消预约**
```
POST http://localhost:9999/self-study/applet/cancel?id=1
Headers: token=你的token值
```

**结束学习**
```
POST http://localhost:9999/self-study/applet/over?id=1
Headers: token=你的token值
```

---

### 👤 用户信息管理

**获取用户信息**
```
GET http://localhost:9999/self-study/applet/getUser
Headers: token=你的token值
```

**修改用户信息**
```
POST http://localhost:9999/self-study/applet/userInfo
Headers: token=你的token值
Body:
{
  "username": "新用户名"
}
```

---

### ✉️ 用户留言

**提交留言**
```
POST http://localhost:9999/self-study/applet/userMessage
Headers: token=你的token值
Body:
{
  "content": "这是我的留言内容"
}
```

---

## 🔧 使用 Postman/Apifox 测试

### 快速开始：

1. **导入 Swagger 文档**
   - 在 Postman/Apifox 中导入 Swagger 文档
   - 地址：`http://localhost:9999/self-study/v2/api-docs`

2. **测试无需登录的接口**
   - 健康检查：`/health`
   - 获取楼层：`/applet/getAllFloor`
   - 获取公告：`/applet/listNotice`

3. **测试需要登录的接口**
   - 先调用登录接口获取 token
   - 在请求头中添加：`token: 返回的token值`

---

## 📊 测试数据

### 系统管理员
```
用户名: admin
密码: admin
```

### 初始化数据
- ✅ **楼层**：5个（一楼~五楼）
- ✅ **自习室**：5个（101、102、201、202、301）
- ✅ **座位**：30个（每个自习室10个座位）
- ✅ **公告**：3条

---

## ✅ 测试检查清单

- [ ] ✅ 访问首页成功
- [ ] ✅ 健康检查接口正常
- [ ] ✅ Swagger 文档可访问
- [ ] ✅ Druid 监控可访问
- [ ] ✅ 获取楼层列表成功
- [ ] ✅ 获取自习室列表成功
- [ ] ✅ 获取座位列表成功
- [ ] ✅ 获取公告列表成功
- [ ] ⏸️ 用户登录（需配置）
- [ ] ⏸️ 创建预约（需登录）
- [ ] ⏸️ 查看预约（需登录）
- [ ] ⏸️ 取消预约（需登录）

---

## 🚀 前端对接示例

### JavaScript/Axios
```javascript
// 获取楼层列表
axios.get('http://localhost:9999/self-study/applet/getAllFloor')
  .then(res => {
    console.log(res.data);
  });

// 创建预约（需要token）
axios.post('http://localhost:9999/self-study/applet/appointment', {
  seatId: 1,
  appointmentDate: '2025-11-28',
  startTime: '08:00',
  endTime: '12:00'
}, {
  headers: { 'token': 'your-token-here' }
})
.then(res => {
  console.log(res.data);
});
```

### 微信小程序
```javascript
// 获取楼层列表
wx.request({
  url: 'http://localhost:9999/self-study/applet/getAllFloor',
  success(res) {
    console.log(res.data);
  }
});

// 创建预约
wx.request({
  url: 'http://localhost:9999/self-study/applet/appointment',
  method: 'POST',
  header: {
    'token': wx.getStorageSync('token')
  },
  data: {
    seatId: 1,
    appointmentDate: '2025-11-28',
    startTime: '08:00',
    endTime: '12:00'
  },
  success(res) {
    console.log(res.data);
  }
});
```

---

## 📝 常见问题

### Q: 访问根路径出现 404？
**A:** 已解决！现在访问 `http://localhost:9999/self-study/` 会显示欢迎页面。

### Q: favicon.ico 404？
**A:** 已添加 SVG 图标，浏览器会自动加载。

### Q: 如何获取 token？
**A:** 调用登录接口后会返回 token，保存后在请求头中使用。

### Q: CORS 跨域问题？
**A:** 如果前端项目遇到跨域问题，可以在配置中添加 CORS 支持。

---

**所有接口已就绪，可以开始测试！** 🎊

**最后更新：** 2025-11-28 10:20

