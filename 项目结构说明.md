# 📁 项目结构说明文档

## 📋 项目概述

本项目采用**前后端分离架构**，包含后端服务（Java）、前端管理系统（Vue）和微信小程序三个子项目。

---

## 🗂️ 根目录结构

```
studyroom/
├── .git/                    # Git 版本控制目录
├── .gitignore              # Git 忽略文件配置
├── .idea/                  # IntelliJ IDEA 项目配置（自动生成）
├── docs/                   # 📚 项目文档目录
├── images/                 # 🖼️ 图片资源（截图、图标等）
├── scripts/                # 🔧 脚本工具目录
├── studyroom-java/         # ☕ 后端项目（Spring Boot）
├── studyroom-vue/          # 🎨 前端项目（Vue.js）
├── studyroom-wx/           # 📱 微信小程序
├── out/                    # 编译输出目录（可删除）
├── target/                 # Maven 编译输出（可删除）
├── package-lock.json       # NPM 依赖锁定文件（可删除）
└── README.md               # 📖 项目主文档
```

---

## 📚 docs/ 文档目录

存放所有项目相关文档，方便管理和查阅。

**推荐结构：**
```
docs/
├── 快速使用指南.md          # 快速入门教程
├── 登录信息.md              # 默认账户密码
├── 系统部署状态.md          # 部署状态说明
├── Git推送指南.md           # Git 使用教程
├── GitHub推送成功.md        # 推送成功记录
├── 分支切换完成.md          # 分支管理说明
├── 快速使用指南-前后端分离.md  # 前后端分离部署指南
├── 快速启动指南-前后端分离.md  # 启动教程
├── 快速命令.txt             # 常用命令集合
├── 手动安装指令.md          # 手动安装步骤
└── api/                     # API 接口文档
    ├── 用户模块.md
    ├── 座位模块.md
    └── 预约模块.md
```

---

## 🔧 scripts/ 脚本目录

存放所有自动化脚本和工具。

**推荐结构：**
```
scripts/
├── 一键启动前后端.bat        # 同时启动前后端
├── 启动Java后端.bat          # 启动后端服务
├── 启动Vue前端.bat           # 启动前端服务
├── 重启后端.bat              # 重启后端服务
├── 快速推送到GitHub.bat      # Git 快速推送
├── 立即安装Vue依赖.bat       # 安装前端依赖
├── 重新安装Vue依赖.bat       # 重装前端依赖
└── database/                 # 数据库相关脚本
    ├── init-db.sql           # 数据库初始化
    ├── test-data.sql         # 测试数据
    └── backup.bat            # 数据库备份脚本
```

---

## ☕ studyroom-java/ 后端项目

基于 **Spring Boot 2.6.6** 的后端服务。

### 目录结构

```
studyroom-java/
├── src/
│   ├── main/
│   │   ├── java/                           # Java 源代码
│   │   │   └── com/selfstudy/
│   │   │       ├── Application.java        # 主启动类
│   │   │       ├── common/                 # 公共模块
│   │   │       │   ├── base/              # 基础类
│   │   │       │   ├── exception/         # 异常处理
│   │   │       │   ├── handler/           # 全局处理器
│   │   │       │   ├── utils/             # 工具类
│   │   │       │   ├── validator/         # 数据校验
│   │   │       │   └── xss/               # XSS 防护
│   │   │       ├── config/                # 配置类
│   │   │       └── modules/               # 业务模块
│   │   │           ├── applet/            # 小程序接口
│   │   │           ├── bas/               # 基础数据
│   │   │           ├── sys/               # 系统管理
│   │   │           └── user/              # 用户管理
│   │   └── resources/                      # 资源文件
│   │       ├── application.yml            # 主配置文件
│   │       ├── application-dev.yml        # 开发环境配置
│   │       ├── application-prod.yml       # 生产环境配置
│   │       ├── logback-spring.xml         # 日志配置
│   │       ├── banner.txt                 # 启动横幅
│   │       ├── mapper/                    # MyBatis 映射文件
│   │       └── static/                    # 静态资源
│   └── test/                               # 测试代码
├── target/                                 # 编译输出（自动生成）
├── pom.xml                                 # Maven 配置
├── db.sql                                  # 数据库初始化脚本
├── db-bas-module.sql                       # 基础模块数据
├── .gitignore                              # Git 忽略配置
├── README.md                               # 后端说明文档
├── start.bat                               # 快速启动脚本
├── quick-check.bat                         # 快速检查脚本
├── 接口测试指南.md                         # API 测试文档
└── 项目启动完整指南.md                     # 启动教程
```

### 核心目录说明

#### 1. common/ 公共模块

| 目录/文件 | 说明 | 主要文件 |
|----------|------|---------|
| `base/` | 基础类和接口 | BaseController.java - 控制器基类<br>PageResult.java - 分页结果<br>QueryInfoDTO.java - 查询参数 |
| `exception/` | 异常处理 | RRException.java - 自定义异常 |
| `handler/` | 全局处理器 | GlobalExceptionHandler.java - 全局异常处理 |
| `utils/` | 工具类集合 | R.java - 统一返回结果<br>PageUtils.java - 分页工具<br>Query.java - 查询构造器<br>Constant.java - 常量定义<br>RedisUtils.java - Redis 工具<br>HttpContextUtils.java - HTTP 上下文<br>IPUtils.java - IP 工具<br>SQLFilter.java - SQL 过滤 |
| `validator/` | 数据校验 | ValidatorUtils.java - 校验工具<br>Assert.java - 断言工具 |
| `xss/` | XSS 防护 | XssFilter.java - XSS 过滤器<br>XssHttpServletRequestWrapper.java - 请求包装<br>HTMLFilter.java - HTML 过滤 |

#### 2. config/ 配置类

| 文件 | 说明 |
|------|------|
| `CorsConfig.java` | 跨域配置 |
| `FilterConfig.java` | 过滤器配置 |
| `KaptchaConfig.java` | 验证码配置 |
| `MessageProperties.java` | 消息配置 |
| `RedisConfig.java` | Redis 配置 |
| `ShiroConfig.java` | Shiro 安全框架配置 |
| `SwaggerConfig.java` | Swagger API 文档配置 |
| `WxMaConfiguration.java` | 微信小程序配置 |

#### 3. modules/ 业务模块

**applet/ - 小程序接口模块**
```
applet/
├── controller/          # 控制器层
│   ├── AppLoginController.java         # 小程序登录
│   ├── AppNoticeController.java        # 公告接口
│   ├── AppRegisterController.java      # 注册接口
│   ├── HealthController.java           # 健康检查
│   └── IndexController.java            # 首页接口
├── service/            # 服务层
│   ├── TbTokenService.java             # Token 服务接口
│   └── impl/
│       ├── TbTokenServiceImpl.java     # Token 服务实现
│       └── UserServiceImpl.java        # 用户服务实现
├── dao/                # 数据访问层
│   ├── TbTokenDao.java                 # Token DAO
│   └── UserDao.java                    # 用户 DAO
├── entity/             # 实体类
│   ├── TbTokenEntity.java              # Token 实体
│   └── UserEntity.java                 # 用户实体
└── interceptor/        # 拦截器
    └── AuthorizationInterceptor.java   # 授权拦截器
```

**bas/ - 基础数据模块**
```
bas/
├── dao/                # 数据访问层
│   ├── BasAppointmentDao.java          # 预约 DAO
│   ├── BasFloorDao.java                # 楼层 DAO
│   ├── BasMessageDao.java              # 消息 DAO
│   ├── BasNoticeDao.java               # 公告 DAO
│   ├── BasSeatDao.java                 # 座位 DAO
│   └── BasStudyRoomDao.java            # 自习室 DAO
├── entity/             # 实体类
│   ├── BasAppointmentEntity.java       # 预约实体
│   ├── BasFloorEntity.java             # 楼层实体
│   ├── BasMessageEntity.java           # 消息实体
│   ├── BasNoticeEntity.java            # 公告实体
│   ├── BasSeatEntity.java              # 座位实体
│   └── BasStudyRoomEntity.java         # 自习室实体
└── service/            # 服务层
    ├── BasAppointmentService.java      # 预约服务接口
    ├── BasFloorService.java            # 楼层服务接口
    └── impl/                            # 服务实现
        ├── BasAppointmentServiceImpl.java
        ├── BasFloorServiceImpl.java
        ├── BasMessageServiceImpl.java
        ├── BasNoticeServiceImpl.java
        ├── BasSeatServiceImpl.java
        └── BasStudyRoomServiceImpl.java
```

**sys/ - 系统管理模块**
```
sys/
├── controller/         # 控制器层
│   └── SysLoginController.java         # 登录控制器
├── dao/                # 数据访问层
│   ├── SysLogDao.java                  # 日志 DAO
│   └── SysUserDao.java                 # 用户 DAO
├── entity/             # 实体类
│   ├── SysLogEntity.java               # 日志实体
│   └── SysUserEntity.java              # 用户实体
├── service/            # 服务层
│   ├── SysLogService.java              # 日志服务接口
│   ├── SysUserService.java             # 用户服务接口
│   └── impl/
│       ├── SysLogServiceImpl.java      # 日志服务实现
│       └── SysUserServiceImpl.java     # 用户服务实现
├── oauth2/             # OAuth2 认证
│   └── TokenGenerator.java             # Token 生成器
└── shiro/              # Shiro 安全
    ├── ShiroUtils.java                 # Shiro 工具类
    └── UserRealm.java                  # 用户领域

**user/ - 用户管理模块**
```
user/
├── dao/                                 # 数据访问层
│   └── TbUserDao.java                  # 用户 DAO
├── entity/                              # 实体类
│   └── TbUserEntity.java               # 用户实体
└── service/                             # 服务层
    ├── TbUserService.java              # 用户服务接口
    └── impl/
        └── TbUserServiceImpl.java      # 用户服务实现
```

#### 4. resources/ 资源目录

| 目录/文件 | 说明 |
|----------|------|
| `application.yml` | 主配置文件，定义公共配置 |
| `application-dev.yml` | 开发环境配置（数据库、Redis等） |
| `application-prod.yml` | 生产环境配置 |
| `logback-spring.xml` | 日志配置（日志级别、输出格式） |
| `banner.txt` | 启动时显示的横幅 |
| `mapper/` | MyBatis XML 映射文件 |
| `static/` | 静态资源（favicon、index.html） |

---

## 🎨 studyroom-vue/ 前端项目

基于 **Vue 2.5.16** 和 **Element UI 2.8.2** 的管理后台。

### 目录结构

```
studyroom-vue/
├── build/                      # Webpack 构建配置
│   ├── build.js               # 生产环境构建脚本
│   ├── check-versions.js      # 版本检查
│   ├── utils.js               # 构建工具函数
│   ├── vue-loader.conf.js     # Vue Loader 配置
│   ├── webpack.base.conf.js   # Webpack 基础配置
│   ├── webpack.dev.conf.js    # 开发环境配置
│   ├── webpack.prod.conf.js   # 生产环境配置
│   └── webpack.test.conf.js   # 测试环境配置
├── config/                     # 项目配置
│   ├── index.js               # 主配置（端口、代理等）
│   ├── dev.env.js             # 开发环境变量
│   ├── prod.env.js            # 生产环境变量
│   └── test.env.js            # 测试环境变量
├── src/                        # 源代码目录
│   ├── App.vue                # 根组件
│   ├── main.js                # 入口文件
│   ├── assets/                # 资源文件
│   │   ├── img/               # 图片资源
│   │   │   ├── avatar.png     # 默认头像
│   │   │   └── login_bg.jpg   # 登录背景
│   │   └── scss/              # 样式文件
│   ├── components/            # 公共组件
│   │   └── icon-svg/          # SVG 图标组件
│   │       ├── index.vue      # 图标组件
│   │       └── index.js       # 组件导出
│   ├── element-ui/            # Element UI 配置
│   │   └── index.js           # Element UI 按需引入
│   ├── element-ui-theme/      # Element UI 主题
│   │   ├── index.js           # 主题配置
│   │   └── element-#xxx/      # 各种主题色
│   ├── icons/                 # 图标配置
│   │   ├── index.js           # 图标注册
│   │   ├── iconfont.js        # 字体图标
│   │   └── svg/               # SVG 图标文件
│   ├── mock/                  # Mock 数据
│   │   ├── index.js           # Mock 配置
│   │   └── modules/           # 各模块 Mock 数据
│   ├── router/                # 路由配置
│   │   ├── index.js           # 路由主配置
│   │   ├── import-development.js  # 开发环境路由导入
│   │   └── import-production.js   # 生产环境路由导入
│   ├── store/                 # Vuex 状态管理
│   │   ├── index.js           # Store 主文件
│   │   └── modules/           # 各模块 Store
│   ├── utils/                 # 工具类
│   │   ├── httpRequest.js     # HTTP 请求封装
│   │   ├── index.js           # 工具函数集合
│   │   └── validate.js        # 表单验证
│   └── views/                 # 页面组件
│       ├── main.vue           # 主布局
│       ├── main-navbar.vue    # 顶部导航栏
│       ├── main-sidebar.vue   # 侧边栏
│       ├── main-content.vue   # 内容区
│       ├── main-navbar-update-password.vue  # 修改密码
│       ├── main-sidebar-sub-menu.vue        # 子菜单
│       └── common/            # 公共页面
│           ├── 404.vue        # 404 页面
│           ├── login.vue      # 登录页面
│           ├── home.vue       # 首页
│           └── theme.vue      # 主题设置
├── static/                     # 静态资源
│   ├── config/                # 前端配置
│   │   ├── index.js           # 开发环境配置
│   │   ├── index-prod.js      # 生产环境配置
│   │   ├── index-qa.js        # QA 环境配置
│   │   ├── index-uat.js       # UAT 环境配置
│   │   └── init.js            # 初始化脚本
│   ├── img/                   # 图片
│   │   └── favicon.ico        # 网站图标
│   └── plugins/               # 第三方插件
│       ├── echarts-3.8.5/     # 图表库
│       ├── mock-1.0.0-beta3/  # Mock.js
│       └── ueditor-1.4.3.3/   # 富文本编辑器
├── test/                       # 测试文件
│   ├── e2e/                   # 端到端测试
│   └── unit/                  # 单元测试
├── .babelrc                    # Babel 配置
├── .editorconfig              # 编辑器配置
├── .eslintignore              # ESLint 忽略
├── .eslintrc.js               # ESLint 配置
├── .gitignore                 # Git 忽略
├── .postcssrc.js              # PostCSS 配置
├── gulpfile.js                # Gulp 配置
├── index.html                 # HTML 模板
├── package.json               # NPM 配置
├── LICENSE                    # 许可证
└── README.md                  # 前端说明
```

### 核心目录说明

#### 1. src/views/ 页面组件

| 文件 | 说明 | 功能 |
|------|------|------|
| `main.vue` | 主布局容器 | 整合导航栏、侧边栏、内容区 |
| `main-navbar.vue` | 顶部导航栏 | 用户信息、退出登录、设置等 |
| `main-sidebar.vue` | 左侧菜单栏 | 菜单导航、路由跳转 |
| `main-content.vue` | 主内容区 | Tab 标签页、页面内容展示 |
| `main-sidebar-sub-menu.vue` | 子菜单组件 | 递归渲染多级菜单 |
| `main-navbar-update-password.vue` | 修改密码弹窗 | 用户修改密码功能 |

#### 2. src/utils/ 工具类

| 文件 | 说明 |
|------|------|
| `httpRequest.js` | HTTP 请求封装（Axios）<br>- 请求/响应拦截器<br>- Token 自动添加<br>- 统一错误处理 |
| `index.js` | 通用工具函数<br>- UUID 生成<br>- 日期格式化<br>- 树形数据处理 |
| `validate.js` | 表单验证规则<br>- URL 验证<br>- 邮箱验证<br>- 手机号验证 |

#### 3. src/router/ 路由配置

| 文件 | 说明 |
|------|------|
| `index.js` | 路由主配置<br>- 全局路由<br>- 主入口路由<br>- 动态路由加载 |
| `import-development.js` | 开发环境路由导入（不使用懒加载） |
| `import-production.js` | 生产环境路由导入（使用懒加载） |

#### 4. static/config/ 前端配置

| 文件 | 说明 |
|------|------|
| `index.js` | 开发环境配置<br>- API 基础路径<br>- CDN 地址 |
| `index-prod.js` | 生产环境配置 |
| `index-qa.js` | QA 测试环境配置 |
| `index-uat.js` | UAT 环境配置 |
| `init.js` | 初始化脚本<br>- 动态加载资源<br>- 插件初始化 |

---

## 📱 studyroom-wx/ 微信小程序

基于微信小程序原生开发。

### 目录结构

```
studyroom-wx/
├── components/             # 自定义组件
│   └── search/            # 搜索组件
│       ├── index.js       # 组件逻辑
│       ├── index.json     # 组件配置
│       ├── index.wxml     # 组件模板
│       └── index.wxss     # 组件样式
├── libs/                  # 第三方库
│   └── vant-ui/          # Vant Weapp UI 组件库
│       ├── action-sheet/
│       ├── button/
│       ├── calendar/
│       └── ...
├── pages/                 # 页面目录
│   └── application/       # 预约页面
│       ├── index.js       # 页面逻辑
│       ├── index.json     # 页面配置
│       ├── index.wxml     # 页面结构
│       └── index.wxss     # 页面样式
├── subpackages/           # 分包页面
│   └── pages/            # 分包页面
├── static/                # 静态资源
│   ├── ali-icon/         # 阿里图标
│   └── image/            # 图片资源
├── utils/                 # 工具类
│   └── util.js           # 通用工具函数
├── app.js                 # 小程序逻辑
├── app.json               # 小程序配置
├── app.wxss               # 小程序样式
├── pages.js               # 页面路径配置
├── project.config.json    # 项目配置
├── project.private.config.json  # 私有配置
├── sitemap.json           # 搜索配置
├── package.json           # NPM 配置
└── LICENSE                # 许可证
```

### 核心文件说明

| 文件 | 说明 |
|------|------|
| `app.js` | 小程序主入口<br>- 生命周期函数<br>- 全局数据 |
| `app.json` | 小程序全局配置<br>- 页面路径<br>- 窗口样式<br>- 底部 Tab |
| `app.wxss` | 全局样式 |
| `pages.js` | 页面路径管理 |
| `project.config.json` | 开发工具配置<br>- AppID<br>- 编译设置 |

---

## 🖼️ images/ 图片目录

存放项目相关图片资源。

**推荐结构：**
```
images/
├── screenshots/            # 系统截图
│   ├── login.jpg          # 登录页面
│   ├── home.jpg           # 首页
│   ├── seat.jpg           # 座位管理
│   └── booking.jpg        # 预约管理
├── wx/                     # 小程序截图
│   ├── wx-home.jpg        # 小程序首页
│   └── wx-booking.jpg     # 预约页面
└── icons/                  # 图标资源
    ├── favicon.ico        # 网站图标
    └── logo.png           # 项目 Logo
```

---

## 🗑️ 可删除的目录/文件

以下目录和文件可以安全删除（会自动重新生成）：

```
studyroom/
├── .idea/                  # IntelliJ IDEA 配置（可重新生成）
├── out/                    # 编译输出（可删除）
├── target/                 # Maven 编译输出（mvn clean 会删除）
├── package-lock.json       # NPM 锁文件（npm install 会重新生成）
└── studyroom-vue/
    └── node_modules/       # NPM 依赖（npm install 会重新下载）
```

---

## 📦 依赖管理

### 后端依赖

- **文件：** `studyroom-java/pom.xml`
- **管理工具：** Maven
- **安装命令：** `mvn clean install`

### 前端依赖

- **文件：** `studyroom-vue/package.json`
- **管理工具：** NPM
- **安装命令：** `npm install`

### 小程序依赖

- **文件：** `studyroom-wx/package.json`
- **管理工具：** NPM
- **安装命令：** `npm install`

---

## 🚀 启动顺序

1. **启动 MySQL 数据库**
2. **启动 Redis**（可选）
3. **启动后端服务**
   ```bash
   cd studyroom-java
   mvn spring-boot:run
   ```
4. **启动前端服务**
   ```bash
   cd studyroom-vue
   npm run dev
   ```

---

## 📝 配置文件优先级

### 后端配置

```
application.yml              # 基础配置（最低优先级）
  ↓
application-dev.yml          # 开发环境配置
  ↓
application-prod.yml         # 生产环境配置
  ↓
命令行参数                   # 最高优先级
```

### 前端配置

```
static/config/index.js       # 开发环境（默认）
  ↓
static/config/index-qa.js    # QA 环境
  ↓
static/config/index-uat.js   # UAT 环境
  ↓
static/config/index-prod.js  # 生产环境
```

---

## 🔍 日志位置

### 后端日志

- **控制台输出：** 实时查看
- **文件输出：** `studyroom-java/logs/`（如果配置了）
- **配置文件：** `logback-spring.xml`

### 前端日志

- **浏览器控制台：** F12 → Console
- **Network 面板：** 查看 HTTP 请求

---

## 🎯 代码规范

### 命名规范

| 类型 | 规范 | 示例 |
|------|------|------|
| **Java 类名** | 大驼峰（PascalCase） | `UserController` |
| **Java 方法/变量** | 小驼峰（camelCase） | `getUserList()` |
| **Java 常量** | 大写下划线 | `MAX_SIZE` |
| **数据库表名** | 小写下划线 | `sys_user` |
| **数据库字段** | 小写下划线 | `user_name` |
| **Vue 组件** | 短横线（kebab-case） | `main-navbar.vue` |
| **JS 变量/函数** | 小驼峰（camelCase） | `getUserInfo()` |

### 目录命名规范

- **Java 包名：** 小写，如 `com.selfstudy.modules.sys`
- **Vue 目录：** 小写，如 `views/`, `components/`
- **文档目录：** 小写，如 `docs/`, `scripts/`

---

## 📌 注意事项

1. **不要提交到 Git 的文件：**
   - `.idea/` - IDE 配置
   - `target/` - 编译输出
   - `node_modules/` - NPM 依赖
   - `*.log` - 日志文件
   - `.DS_Store` - Mac 系统文件

2. **数据库配置：**
   - 默认配置在 `application-dev.yml`
   - 生产环境需修改 `application-prod.yml`
   - 不要提交包含真实密码的配置文件

3. **端口配置：**
   - 后端：`9999`
   - 前端：`8001` 或 `8004`（自动分配）
   - MySQL：`3306`
   - Redis：`6379`

---

## 📖 相关文档

- [快速使用指南](快速使用指南.md)
- [Git 推送指南](Git推送指南.md)
- [接口测试指南](studyroom-java/接口测试指南.md)
- [项目启动完整指南](studyroom-java/项目启动完整指南.md)

---

**文档版本：** 1.0  
**最后更新：** 2025-11-28  
**维护者：** [@ffzzffz](https://github.com/ffzzffz)

